<!-- Sidebar -->
<aside class="sidebar">
    <div class="sidebar-brand">
      <h3 class="m-0 text-primary">DriveEase Admin</h3>
    </div>
    
    <div class="sidebar-menu">
      <a href="/admin" class="sidebar-link <%= title === 'Dashboard' ? 'active' : '' %>">
        <i class="fas fa-tachometer-alt"></i>
        <span>Dashboard</span>
      </a>
      <a href="/admin/vehicles" class="sidebar-link <%= title === 'Vehicle Management' ? 'active' : '' %>">
        <i class="fas fa-car"></i>
        <span>Vehicle Management</span>
      </a>
      <a href="/admin/bookings" class="sidebar-link <%= title === 'Bookings' ? 'active' : '' %>">
        <i class="fas fa-calendar-check"></i>
        <span>Bookings</span>
      </a>
      <a href="/admin/users" class="sidebar-link <%= title === 'User Management' ? 'active' : '' %>">
        <i class="fas fa-users"></i>
        <span>User Management</span>
      </a>
      <a href="/admin/payments" class="sidebar-link <%= title === 'Payments' ? 'active' : '' %>">
        <i class="fas fa-credit-card"></i>
        <span>Payments</span>
      </a>
      <a href="/admin/reports" class="sidebar-link <%= title === 'Reports' ? 'active' : '' %>">
        <i class="fas fa-chart-bar"></i>
        <span>Reports</span>
      </a>
      <a href="/admin/settings" class="sidebar-link <%= title === 'Settings' ? 'active' : '' %>">
        <i class="fas fa-cog"></i>
        <span>Settings</span>
      </a>
    </div>
  </aside>
  <style>
    /* Sidebar Styles */
.sidebar {
  width: 250px;
  min-height: 100vh;
  background: #2c3e50;
  color: white;
  transition: all 0.3s;
  position: fixed;
  z-index: 1000;
}

.sidebar-header {
  padding: 20px;
  background: #1a252f;
}

.sidebar-menu {
  padding: 0;
  list-style: none;
}

.sidebar-menu li {
  padding: 10px 20px;
  border-bottom: 1px solid rgba(255,255,255,0.1);
}

.sidebar-menu li a {
  color: #ecf0f1;
  text-decoration: none;
  display: block;
}

.sidebar-menu li a:hover {
  color: #3498db;
}

.sidebar-menu li.active {
  background: rgba(255,255,255,0.1);
}

/* Main content area adjustment */
.main-content {
  margin-left: 250px;
  width: calc(100% - 250px);
  transition: all 0.3s;
}

/* Collapsed state */
.sidebar-collapsed .sidebar {
  width: 80px;
}

.sidebar-collapsed .main-content {
  margin-left: 80px;
  width: calc(100% - 80px);
}

/* Responsive */
@media (max-width: 768px) {
  .sidebar {
    width: 80px;
  }
  .main-content {
    margin-left: 80px;
  }
  .sidebar-header span {
    display: none;
  }
  .sidebar-menu li span {
    display: none;
  }
}
  </style>